sg-compositor (0.2.0+sg6) unstable; urgency=medium

  * Elevated programs' displays (ADR 0012, B56): ELEVATED on the control
    socket (the lock account or root, with the X server's Wayland and
    window-manager connections and a readiness pipe) makes this compositor
    the window manager of an elevated program's own Xwayland, run as SYSTEM
    by sg-session's sg-elevated-run. Its windows are managed (placed, moved
    and resized by _NET_WM_MOVERESIZE, minimised, maximised), stacked above
    the session's and below privileged views, hidden while locked. Alt+Tab
    leaves them for the session; ACTIVATE (the taskbar) and WINDOWS.
    Clipboard: elevated -> session text; session -> elevated text only on
    the user's own key or click in it; no drag and drop.
    Gate: make test-elevated (a session adversary's XTEST, XSendEvent and
    XGetImage reach nothing of it), mutant-proven.

 -- Stained Glass OS <ke7oxh@gmail.com>  Sat, 26 Sep 2026 00:45:00 -0700

sg-compositor (0.2.0+sg5) unstable; urgency=medium

  * Display power: wlr-output-power-management-unstable-v1, so wlopm and
    Settings' "Turn off the screen after" (swayidle) turn the screen off; the
    output is disabled in place (no window moves, XWayland keeps its size)
    and any input turns it back on. The output a Remote Desktop session is
    captured from is never turned off. Gate: make test-power (swayidle
    timeouts, input resetting them, idle inhibitors).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 11:20:00 -0700

sg-compositor (0.2.0+sg4) unstable; urgency=medium

  * Remote Desktop takes over the console session (E1b): REMOTE on the control
    socket (lock account or root; optionally with the RDP daemon's end of a
    socketpair as the remote connection) moves the user's windows to a new
    headless output at the console's size, unlocks, and ignores the console's
    own keyboard, pointer and touch; Ctrl+Alt+Del at the console, LOCAL, or
    the remote connection closing gives the session back locked, and the
    remote connection loses it at once. view_center honours the output's
    offset.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 13:02:27 -0700

sg-compositor (0.2.0+sg3) unstable; urgency=medium

  * An output with no modes of its own (headless: a Remote Desktop session)
    takes its size from SG_OUTPUT_SIZE=WxH, the RDP client's desktop size,
    instead of wlroots' fixed 1280x720.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 07:11:39 -0700

sg-compositor (0.2.0+sg2) unstable; urgency=medium

  * SECURE/RELEASE: isolate the session for elevation consent prompts
    without a lock screen (ADR 0012).
  * A control client that hangs up early no longer kills the compositor
    (SIGPIPE on the reply).

 -- Stained Glass OS <ke7oxh@gmail.com>  Wed, 23 Sep 2026 22:48:07 -0700

sg-compositor (0.2.0+sg1) unstable; urgency=medium

  * Initial package: cage 0.2.0 renamed, hosting the Stained Glass session.

 -- Stained Glass OS <dev@stained-glass.example>  Tue, 22 Sep 2026 13:53:48 -0700
